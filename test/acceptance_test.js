import {expect} from 'chai';
import {
} from '../src/complex';

describe('complexjs', () => {

  before(() => {
  });

  it('handles operations mixing euler and polar forms', () => {
    expect(true).to.equal(true);
  });

});
